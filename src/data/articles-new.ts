import { Article } from './types';

/**
 * Add new articles here.
 * Note: Following the project guidelines, articles should not include a "Conclusion" section.
 */
export const newArticles: Article[] = [];
